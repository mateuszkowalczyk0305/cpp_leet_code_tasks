#include <iostream>
#include <vector>

std::vector<int> plusOne(std::vector<int> &digits) {}

int main() {
  std::vector<int> a = {1, 2, 3};
  std::vector<int> c = {4, 3, 2, 1};
  std::vector<int> b = {9};
  return 0;
}